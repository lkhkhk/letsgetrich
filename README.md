#

## Python 가상환경
```pwsh
uv venv
.\.venv\Scripts\activate
```

## git 초기화
```pwsh
git init

git add .
git commit -m "init"

# git remote add origin https://github.com/[사용자ID]/[저장소명].git

git push -u origin main

```